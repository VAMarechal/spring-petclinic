pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo "${params.Stored_images}"
                echo "${Stored_images}"
            }
        }
    }
}
