pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo "${params.selected_image}"
                echo "${selected_image}"
            }
        }
    }
}
